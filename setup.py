from setuptools import setup, find_packages

setup(
    name="energy_predictor",
    version="1.0",
    packages=find_packages(),
    install_requires=[
        # List your dependencies here in the format:
        # 'package_name==version',
    ],
)
